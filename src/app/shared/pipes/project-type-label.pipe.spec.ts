import { ProjectTypeLabelPipe } from './project-type-label.pipe';

describe('ProjectTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
