import { ResearchGroupsLabelPipe } from './research-groups-label.pipe';

describe('ResearchGroupsLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ResearchGroupsLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
