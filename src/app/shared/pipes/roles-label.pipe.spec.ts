import { RolesLabelPipe } from './roles-label.pipe';

describe('RolesLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new RolesLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
